#import "../thesis-base.typ": *
#import "@preview/codly:1.3.0": *
#import "@preview/codly-languages:0.1.1": *

#show: codly-init.with()
#codly(languages: codly-languages, breakable: true)

#chapter(4, "Экспериментальная оценка")

Данная глава содержит описание тестовой среды, результаты верификации на ручных бенчмарках из базы Alive2, анализ производительности CHC-решателей и результаты масштабной оценки с помощью генератора случайных программ Csmith.

== Тестовая среда и инструментарий

Все эксперименты проводились на рабочей станции, конфигурация которой приведена в таблице @table-testenv.

#figure(
  caption: [Конфигурация тестового стенда],
  table(
    columns: (auto, auto),
    table.header([Параметр], [Значение]),
    [Процессор], [12th Gen Intel Core i5-1235U],
    [Оперативная память], [8 ГБ],
    [Операционная система], [Arch Linux (ядро Linux 6.18.7-arch1-1, x86\_64)],
    [CHC-решатель], [Z3 4.15.4 (64-бит)],
    [Реализация RICOVER], [Rust 1.89.0-nightly (edition 2024)],
    [Компилятор LLVM/Clang], [23.0.0git (цель: riscv64-linux-gnu)],
    [Генератор программ Csmith], [версия 2.4.0],
  ),
) <table-testenv>

Стратегия CHC-решателя --- Spacer, параметр `set-logic HORN`. Тайм-аут Z3 для экспериментов с Csmith --- 300 секунд (5 минут) на одну задачу.

== Тестовая база: ошибки мисоптимизации из Alive2

Для первичной верификации корректности RICOVER использовалась тестовая база, составленная из подтверждённых ошибок мисоптимизации компилятора LLVM, обнаруженных инструментом Alive2 @cite1. Каждая такая ошибка сопровождается тремя элементами:

+ исходный вариант кода (до оптимизации);
+ _ошибочная_ оптимизация (с мисоптимизацией);
+ _исправленная_ оптимизация (корректный вариант).

Методика верификации: RICOVER должен вернуть `sat` (расхождение обнаружено) для пары «до / ошибочная оптимизация» и `unsat` (эквивалентность подтверждена) для пары «до / исправленная оптимизация». Только оба результата вместе подтверждают корректность работы инструмента.

Воспроизведение ошибок Alive2 на уровне RISC-V ассемблера требовало дополнительного шага: исходные ошибки описаны на уровне LLVM IR, и их необходимо воспроизвести путём компиляции конкретным LLVM-бэкендом для RISC-V.

В директории `benchmark/supported/` представлены следующие верифицированные бенчмарки:

- `bench1_BAD.s` --- ошибочная оптимизация (ожидаемый результат: `sat`);
- `constant_folding_simple_SUCCESS.s` --- корректная константная свёртка (`unsat`);
- `compressed_equiv_SUCCESS.s` --- эквивалентность со сжатыми инструкциями (`unsat`);
- `amo_swap_SUCCESS.s` --- атомарная обменная операция (`unsat`);
- `PR44306_BAD.s` --- воспроизведение бага PR44306 из трекера LLVM (`sat`).

Результаты приведены в таблице @table-benchmarks.

#figure(
  caption: [Результаты верификации ручных бенчмарков],
  table(
    columns: (auto, auto, auto, auto, auto),
    table.header([Бенчмарк], [Тип], [Инструкций (src/tgt)], [Результат Z3], [Время решения]),
    [constant\_folding], [UNSAT], [14 / 2], [*unsat*], [< 1 с],
    [compressed\_equiv], [UNSAT], [10 / 8], [*unsat*], [< 1 с],
    [amo\_swap], [UNSAT], [6 / 4], [*unsat*], [< 1 с],
    [bench1], [SAT (ошибка)], [12 / 8], [*sat*], [< 2 с],
    [PR44306], [SAT (ошибка)], [18 / 10], [*sat*], [< 2 с],
  ),
) <table-benchmarks>

Все поддерживаемые бенчмарки дают ожидаемый результат. Ручные бенчмарки разрешаются менее чем за 2 секунды: они имеют небольшое число инструкций и не затрагивают агрессивных операций с памятью, которые усложняют задачу для массивной теории.

В бенчмарке `constant_folding` функция `foo1` вычисляет $y = x + 2$ при $x = 1$ через стек (14 инструкций); функция `foo2` возвращает `addi a0, zero, 3; ret` (2 инструкции). Z3 отвечает `unsat` мгновенно.

В бенчмарке `bench1` (sat) воспроизводит конкретный баг LLVM. Z3 отвечает `sat` и предоставляет конкретное начальное состояние (модель), в котором оптимизированный вариант вычисляет иной результат, что позволяет воспроизвести ошибку без перебора тестовых входных данных.

== Производительность CHC-решателей

На генерируемых Csmith функциях производительность Z3 существенно зависит от сложности CHC-задачи. Ключевой метрикой сложности является не общее число инструкций, а *число операций с памятью* (load/store: `lb`, `lh`, `lw`, `ld`, `sb`, `sh`, `sw`, `sd`) в исходной функции. Каждая такая операция порождает ограничение над массивной теорией (`Array(BitVec 64 → BitVec 8)`), которое является основным источником вычислительной сложности для решателя.

#figure(
  caption: [Зависимость результата Z3 от числа операций с памятью (20 программ, instcombine, тайм-аут 300 с)],
  table(
    columns: (auto, auto, auto, auto, auto),
    table.header([Операций с памятью (src)], [Протестировано], [Решено], [Тайм-аут], [Доля решённых]),
    [3--8], [4], [4], [0], [100%],
    [9--11], [7], [7], [0], [100%],
    [12], [1], [1], [0], [100%],
    [13+], [5], [0], [4], [0%],
  ),
) <table-memops>

Практическая граница разрешимости --- *около 12 операций с памятью* в исходной функции. Чисто регистровые функции решаются за секунды независимо от числа инструкций.

Два фактора, расширяющие границу разрешимости:

+ *Оптимизация устранения переменных памяти.* До оптимизации функция из 20 инструкций порождала 43 ограничения над массивами; после оптимизации --- 10. Снижение числа array-ограничений в 4,3 раза кратно ускоряет поиск инварианта.

+ *Инлайнинг правил инструкций.* Вместо обращения к инструкционным отношениям через `declare-rel` тела правил встраиваются непосредственно в программное правило, устраняя слой вложенности CHC-системы и упрощая работу алгоритма Spacer @cite14.

+ *Пересылка store--load (store-load forwarding).* Код уровня O0 порождает обильные паттерны вида «записать значение в стек --- немедленно считать из того же слота»:

```asm
sw a0, -20(s0)    ; записать a0 в слот стека
lw a0, -20(s0)    ; считать обратно (избыточно)
```

Каждое чтение из памяти порождает ограничение `read_mem` над массивной теорией. RICOVER выполняет *пересылку store--load* на этапе эмиссии CHC: если инструкция загрузки (`lw`, `ld`, ...) читает из слота, в который непосредственно перед этим была выполнена запись (`sw`, `sd`, ...) с тем же смещением, базовым регистром и шириной, ограничение `read_mem` заменяется прямым выражением над регистром-источником. Пересылка консервативна: она отменяется при модификации базового или исходного регистра, а также при любой записи в память по другому базовому адресу (conservative aliasing).

Результаты оценки оптимизации пересылки store--load на 100 программах Csmith (проход instcombine, строгие флаги, тайм-аут 300 с) приведены в таблице @table-forwarding.

#figure(
  caption: [Зависимость результата Z3 от числа операций с памятью (100 программ, instcombine, с пересылкой store--load)],
  table(
    columns: (auto, auto, auto, auto, auto),
    table.header([Операций с памятью (src)], [Протестировано], [Решено], [Тайм-аут], [Доля решённых]),
    [3--5], [8], [8], [0], [100%],
    [6--8], [23], [23], [0], [100%],
    [9--11], [22], [21], [1], [95%],
    [12--14], [12], [6], [6], [50%],
    [15--17], [16], [4], [12], [25%],
    [18--20], [9], [3], [6], [33%],
    [21+], [16], [0], [16], [0%],
  ),
) <table-forwarding>

Всего протестировано 126 функций: 60 EQUIV, 5 SAT (ложные срабатывания по предусловиям ABI), 42 тайм-аута, 19 пропущено (неподдерживаемые инструкции). Медиана времени решения: 63,9 с, максимум: 293,2 с.

Сравнение с базовой конфигурацией (без пересылки store--load) приведено в таблице @table-forwarding-cmp.

#figure(
  caption: [Сравнение результатов с пересылкой store--load и без неё],
  table(
    columns: (auto, auto, auto, auto),
    table.header([Метрика], [Без пересылки], [С пересылкой], [Улучшение]),
    [Практическая граница], [$approx$ 12 мем. оп.], [$approx$ 15--18 мем. оп.], [+25--50%],
    [Доля решённых (9--11 оп.)], [100%], [95%], [$approx$ то же],
    [Доля решённых (12--14 оп.)], [$approx$ 0%], [50%], [новая возможность],
    [Доля решённых (15--17 оп.)], [0%], [25%], [новая возможность],
    [Доля решённых (18--20 оп.)], [0%], [33%], [новая возможность],
    [Макс. решённых мем. оп.], [12], [20], [+67%],
  ),
) <table-forwarding-cmp>

Оптимизация расширяет границу разрешимости с $approx$ 12 до $approx$ 15--18 операций с памятью, с отдельными функциями до 20 мем. оп. (например, `func_48`: 20 оп., решено за 293 с; `func_21`: 18 оп., решено за 275 с).

При включении указателей (`--relaxed`) генерируются функции с интенсивным использованием памяти. Результаты на 20 программах приведены в таблице @table-forwarding-relaxed.

#figure(
  caption: [Результаты с пересылкой store--load на расслабленных Csmith-программах (20 программ, instcombine, тайм-аут 300 с)],
  table(
    columns: (auto, auto, auto, auto, auto),
    table.header([Операций с памятью (src)], [Протестировано], [Решено], [Тайм-аут], [Доля решённых]),
    [6--8], [2], [2], [0], [100%],
    [9--11], [5], [4], [1], [80%],
    [12--14], [1], [1], [0], [100%],
    [18--20], [3], [0], [3], [0%],
    [21--35], [6], [0], [6], [0%],
    [48--62], [4], [2], [2], [50%],
    [96+], [3], [0], [1], [0%],
  ),
) <table-forwarding-relaxed>

Всего 28 функций протестировано: 8 EQUIV, 1 SAT (ложное срабатывание), 13 тайм-аутов, 4 пропущено, 2 ошибки. Медиана: 122,6 с, p90: 256,4 с. Пересылка store--load значительно снижает число эффективных операций с памятью в целевой функции (большинство функций показывают 4 мем. оп. после пересылки), однако сложность исходной стороны остаётся доминирующим фактором. Практическая граница в расслабленном режиме --- $approx$ 12 операций с памятью в источнике.

=== Конфигурация решателя Spacer

Z3 при указании `set-logic HORN` автоматически активирует движок Spacer. В экспериментах использовалась версия Z3 4.15.4 с параметрами по умолчанию. Ключевые параметры Spacer приведены в таблице @table-spacer.

#figure(
  caption: [Параметры Z3 Spacer, используемые по умолчанию],
  table(
    columns: (auto, auto, auto),
    table.header([Параметр], [Значение], [Описание]),
    [`spacer.global`], [false], [Стратегия глобального руководства (global guidance)],
    [`spacer.mbqi`], [true], [Model-based quantifier instantiation],
    [`spacer.q3`], [true], [Квантификаторная обработка],
    [`spacer.use_array_eq_generalizer`], [true], [Обобщение равенств массивов],
    [`xform.inline_eager`], [true], [Жадный инлайнинг отношений],
    [`xform.inline_linear`], [true], [Инлайнинг линейных правил],
    [`xform.inline_linear_branch`], [false], [Инлайнинг линейных ветвящихся правил],
    [`xform.coalesce_rules`], [false], [Слияние правил],
    [`xform.array_blast`], [false], [Развёртка массивов в скаляры],
  ),
) <table-spacer>

Для оценки влияния параметров Spacer на производительность было проведено сравнительное исследование 7 конфигураций на трёх наборах данных: ручные бенчмарки, строгие Csmith-функции и расслабленные Csmith-функции (с указателями). Результаты приведены в таблице @table-spacer-exp.

#figure(
  caption: [Сравнение конфигураций Spacer (8 запросов, 8--17 операций с памятью, тайм-аут 300 с)],
  table(
    columns: (1fr, auto, auto, auto),
    table.header([Конфигурация], [Решено], [Медиана], [Примечание]),
    [baseline (по умолчанию)], [5 / 8], [117,9 с], [---],
    [`spacer.global=true`], [5 / 8], [118,0 с], [$approx$ 0%],
    [`xform.inline_linear_branch=true`], [5 / 8], [118,2 с], [$approx$ 0%],
    [`xform.coalesce_rules=true`], [5 / 8], [121,0 с], [+3%],
    [`spacer.global` + inline + coalesce], [5 / 8], [118,5 с], [$approx$ 0%],
    [`array_blast=true`], [5 / 8], [78,1 с#super("*")], [+63% на простых],
    [`use_array_eq_generalizer=false`], [5 / 8], [117,9 с], [+12% на сложных],
  ),
) <table-spacer-exp>

#super("*")Значение для набора с лёгкими запросами; на сложных запросах `array_blast` ухудшает производительность.

Ни одна конфигурация не изменила множество решаемых запросов: все 7 вариантов решили одни и те же 5 из 8 запросов. Вариация времени между конфигурациями (до $plus.minus$15%) меньше вариации между отдельными запросами (до $plus.minus$60%). Стратегия global guidance (`spacer.global`), на которую возлагались надежды, не оказала значимого влияния на структуру CHC, порождаемую RICOVER. Развёртка массивов (`array_blast`) активно вредит --- преобразование `Array(BitVec 64 → BitVec 8)` в скалярные переменные экспоненциально увеличивает число переменных. Конфигурация по умолчанию является близкой к оптимальной для данного класса CHC-задач.

== Масштабная верификация с помощью Csmith

Для систематической оценки применимости подхода к реальным компиляторным оптимизациям была разработана система автоматизированного тестирования на основе генератора случайных программ Csmith @cite3. Конвейер `csmith_ricover_e2e.py` функционирует следующим образом:

```
csmith --no-safe-math --no-pointers --no-global-variables
    -> program.c
    -> clang -O0 -Xclang -disable-O0-optnone -emit-llvm -S -> base.ll
    -> llc -O1 -> base.s
    -> opt -passes='<pipeline>' -S -> opt.ll -> llc -O1 -> opt.s
    -> пофункциональный diff (пропуск: call, relocations, >max-instrs)
    -> RICOVER check-equiv -> .smt2
    -> z3 -T:300 -> sat/unsat/timeout
```

Флаг `-Xclang -disable-O0-optnone` необходим, чтобы `opt` проходы фактически срабатывали на O0 IR. Флаг `--no-global-variables` позволяет избежать PC-relative relocations (`auipc`/`%pcrel`), которые RICOVER пока не поддерживает.

*Тестируемые проходы LLVM:* instcombine, simplifycfg, gvn, dse, licm, indvars, loop-rotate, loop-unroll, loop-deletion --- всего 9 конвейеров.

Из 185 функций с различиями между базовым и оптимизированным вариантами (20 программ, проход instcombine):

#figure(
  caption: [Воронка отбора функций (проход instcombine, 20 программ)],
  table(
    columns: (auto, auto, auto),
    table.header([Стадия фильтрации], [Количество], [Доля от различающихся]),
    [Функции с различиями], [185], [100%],
    [Заблокированы инструкцией `call`], [152], [82%],
    [Заблокированы PC-relative relocation], [136], [73%],
    [Без вызовов и relocations], [20], [11%],
    [В пределах размера + поддерживаемые инструкции], [11], [6%],
  ),
) <table-funnel>

За серию из 100 программ Csmith (проход instcombine, строгие флаги, тайм-аут 300 с, с пересылкой store--load) было протестировано 107 функций с различиями между базовым и оптимизированным вариантами. Репрезентативные примеры из различных диапазонов сложности приведены в таблице @table-csmith.

#figure(
  caption: [Репрезентативные результаты верификации функций Csmith (100 программ, instcombine, с пересылкой store--load)],
  table(
    columns: (auto, auto, auto, auto, auto, auto),
    table.header([Категория], [Функция], [src / tgt инструкций], [Мем. оп. (src / tgt)], [Результат Z3], [Время]),
    [Быстрая (регистровая)], [func\_5], [41 / 6], [19 / 2], [*unsat*], [0,1 с],
    [Типичная], [func\_70], [15 / 9], [9 / 4], [*unsat*], [63,9 с],
    [Граничная], [func\_21], [35 / 9], [18 / 4], [*unsat*], [274,5 с],
    [Граничная], [func\_48], [37 / 10], [20 / 4], [*unsat*], [293,2 с],
    [Ложное сраб. (ABI)], [func\_54], [26 / 9], [16 / 4], [*sat*], [0,2 с],
    [Тайм-аут], [func\_11], [20 / 10], [12 / 4], [timeout], [300 с],
  ),
) <table-csmith>

Функция `func_5` (19 операций с памятью в источнике, 2 после пересылки) решается мгновенно --- пересылка store--load устраняет почти все array-ограничения. Функция `func_48` (20 мем. оп.) --- наиболее сложная из решённых, на границе тайм-аута (293 с). Результат `sat` для `func_54` --- ложное срабатывание по предусловиям ABI (см. раздел «Ограничения»). Всего из 107 протестированных функций: 60 подтверждены эквивалентными (`unsat`), 5 ложных срабатываний (`sat`), 42 превысили тайм-аут (подробная разбивка по числу операций с памятью --- в @table-forwarding).

*Результаты прогона по всем 9 проходам* (4 программы):

#figure(
  caption: [Результаты прогона по всем 9 проходам LLVM (12 программ Csmith, тайм-аут 300 с)],
  table(
    columns: (auto, auto, auto, auto),
    table.header([Проход LLVM], [Протестировано], [EQUIV], [Тайм-аут]),
    [instcombine], [14], [12], [2],
    [simplifycfg], [12], [10], [2],
    [gvn], [12], [10], [2],
    [dse], [12], [10], [2],
    [licm], [12], [10], [2],
    [indvars], [12], [10], [2],
    [loop-rotate], [12], [10], [2],
    [loop-unroll], [12], [10], [2],
    [loop-deletion], [12], [10], [2],
    [*Итого*], [*110*], [*92*], [*18*],
  ),
) <table-allpasses>

92 из 110 тестируемых случаев подтверждены как эквивалентные (84%). Ни одного `sat` --- все 9 проходов LLVM производят семантически корректные оптимизации. 18 тайм-аутов приходятся на две функции с 17 и 21 операцией с памятью, стабильно превышающие 300-секундный порог независимо от прохода.

При включении флага `--relaxed` (указатели разрешены, `--no-pointers` снят) генерируются более сложные функции с интенсивным использованием памяти. За серию из 30 программ (все 9 проходов, тайм-аут 300 с, `--max-mem-ops 21`) пять функций оказались доступны для верификации. Результаты приведены в таблице @table-allpasses-relaxed.

#figure(
  caption: [Результаты прогона по всем 9 проходам LLVM (--relaxed, 30 программ, тайм-аут 300 с)],
  table(
    columns: (auto, auto, auto, auto, auto, auto),
    table.header([Программа], [Функция], [src / tgt инструкций], [Мем. оп. (src / tgt)], [Проходов], [Результат]),
    [prog\_002], [func\_1],  [17 / 10], [8 / 4],  [9], [*9/9 EQUIV*],
    [prog\_004], [func\_77], [15 / 11], [8 / 4],  [9], [*9/9 EQUIV*],
    [prog\_009], [func\_1],  [12 / 10], [17 / 4],  [9], [*9/9 EQUIV*],
    [prog\_004], [func\_93], [30 / 16], [21 / 8], [9], [9/9 тайм-аут],
    [prog\_006], [func\_58], [28 / 10], [24 / 8], [9], [9/9 тайм-аут],
  ),
) <table-allpasses-relaxed>

Функции с 6--8 операциями с памятью подтверждены эквивалентными по всем 9 проходам; функции с 22--24 операциями стабильно превышают тайм-аут на каждом из проходов. Результат согласуется с границей разрешимости, установленной в @table-forwarding. 

== Ограничения и направления развития

Известные ограничения прототипа:

+ *Масштабируемость решателя.* Оптимизация пересылки store--load расширила границу сложности с $approx$ 12 до $approx$ 15--18 операций с памятью в исходной функции (см. @table-forwarding-cmp), однако функции выше этого предела по-прежнему стабильно превышают тайм-аут 300 секунд. Настройка параметров Spacer не меняет границу разрешимости (см. @table-spacer-exp). Направление развития: применение Eldarica @cite13 как альтернативного CHC-решателя с иной стратегией поиска инварианта, а также дальнейшая редукция array-ограничений.

+ *Функции с вызовами (call).* Csmith генерирует функции, вызывающие другие функции программы. RICOVER верифицирует одиночные функции и не поддерживает межпроцедурный анализ. Направление: compositional verification, верификация пре-/постусловий вызываемых функций.

+ *PC-relative relocations.* Функции, использующие `auipc`/`%pcrel` для загрузки глобальных переменных, не поддерживаются. Направление: расширение модели состояния на сегмент глобальных данных.

+ *Ложные срабатывания (false positives).* Идентифицировано два класса:
  - _Предусловия ABI:_ RICOVER не ограничивает начальные значения регистров вызывным соглашением. Функции с узкими типами параметров (`uint8_t`, `int16_t`) могут давать `sat`, потому что O0 явно расширяет знак, а оптимизированная версия предполагает гарантию ABI.
  - _Мёртвые записи в стек по значению:_ при передаче структуры по значению DSE корректно удаляет мёртвую запись в локальную копию структуры, но эта копия находится за пределами приватного фрейма (на стеке вызывающей функции), и RICOVER считает её наблюдаемой.
