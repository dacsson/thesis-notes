#import "../thesis-base.typ": *
#import "@preview/codly:1.3.0": *
#import "@preview/codly-languages:0.1.1": *

#show: codly-init.with()
#codly(languages: codly-languages, breakable: true)

#chapter(4, "Экспериментальная оценка")

Данная глава содержит описание тестовой среды, результаты верификации на ручных бенчмарках из базы Alive2, анализ производительности CHC-решателей и результаты масштабной оценки с помощью генератора случайных программ Csmith.

== Тестовая среда и инструментарий

Все эксперименты проводились на рабочей станции, конфигурация которой приведена в таблице @table-testenv.

#figure(
  caption: [Конфигурация тестового стенда],
  table(
    columns: (auto, auto),
    table.header([Параметр], [Значение]),
    [Процессор], [12th Gen Intel Core i5-1235U],
    [Оперативная память], [8 ГБ],
    [Операционная система], [Arch Linux (ядро Linux 6.18.7-arch1-1, x86\_64)],
    [CHC-решатель], [Z3 4.15.4 (64-бит)],
    [Реализация RICOVER], [Rust 1.89.0-nightly (edition 2024)],
    [Компилятор LLVM/Clang], [23.0.0git (цель: riscv64-linux-gnu)],
    [Генератор программ Csmith], [версия 2.4.0],
  ),
) <table-testenv>

Стратегия CHC-решателя --- Spacer, параметр `set-logic HORN`. Тайм-аут Z3 для экспериментов с Csmith --- 300 секунд (5 минут) на одну задачу.

== Тестовая база: ошибки мисоптимизации из Alive2

Для первичной верификации корректности RICOVER использовалась тестовая база, составленная из подтверждённых ошибок мисоптимизации компилятора LLVM, обнаруженных инструментом Alive2 @cite1. Каждая такая ошибка сопровождается тремя элементами:

+ исходный вариант кода (до оптимизации);
+ _ошибочная_ оптимизация (с мисоптимизацией);
+ _исправленная_ оптимизация (корректный вариант).

Методика верификации: RICOVER должен вернуть `sat` (расхождение обнаружено) для пары «до / ошибочная оптимизация» и `unsat` (эквивалентность подтверждена) для пары «до / исправленная оптимизация». Только оба результата вместе подтверждают корректность работы инструмента.

Воспроизведение ошибок Alive2 на уровне RISC-V ассемблера требовало дополнительного шага: исходные ошибки описаны на уровне LLVM IR, и их необходимо воспроизвести путём компиляции конкретным LLVM-бэкендом для RISC-V.

В директории `benchmark/supported/` представлены следующие верифицированные бенчмарки:

- `bench1_BAD.s` --- ошибочная оптимизация (ожидаемый результат: `sat`);
- `constant_folding_simple_SUCCESS.s` --- корректная константная свёртка (`unsat`);
- `compressed_equiv_SUCCESS.s` --- эквивалентность со сжатыми инструкциями (`unsat`);
- `amo_swap_SUCCESS.s` --- атомарная обменная операция (`unsat`);
- `PR44306_BAD.s` --- воспроизведение бага PR44306 из трекера LLVM (`sat`).

Результаты приведены в таблице @table-benchmarks.

#figure(
  caption: [Результаты верификации ручных бенчмарков],
  table(
    columns: (auto, auto, auto, auto, auto),
    table.header([Бенчмарк], [Тип], [Инструкций (src/tgt)], [Результат Z3], [Время решения]),
    [constant\_folding\_simple], [UNSAT], [14 / 2], [*unsat*], [< 1 с],
    [compressed\_equiv], [UNSAT], [10 / 8], [*unsat*], [< 1 с],
    [amo\_swap], [UNSAT], [6 / 4], [*unsat*], [< 1 с],
    [bench1], [SAT (ошибка)], [12 / 8], [*sat*], [< 2 с],
    [PR44306], [SAT (ошибка)], [18 / 10], [*sat*], [< 2 с],
  ),
) <table-benchmarks>

Все поддерживаемые бенчмарки дают ожидаемый результат. Ручные бенчмарки разрешаются менее чем за 2 секунды: они имеют небольшое число инструкций и не затрагивают агрессивных операций с памятью, которые усложняют задачу для массивной теории.

*Бенчмарк constant_folding_simple.* Функция `foo1` вычисляет $y = x + 2$ при $x = 1$ через стек (14 инструкций); функция `foo2` возвращает `addi a0, zero, 3; ret` (2 инструкции). Z3 отвечает `unsat` мгновенно.

*Бенчмарк bench1 (sat).* Воспроизводит конкретный баг LLVM. Z3 отвечает `sat` и предоставляет конкретное начальное состояние (модель), в котором оптимизированный вариант вычисляет иной результат, что позволяет воспроизвести ошибку без перебора тестовых входных данных.

== Производительность CHC-решателей

На генерируемых Csmith функциях производительность Z3 существенно зависит от сложности CHC-задачи. Ключевые метрики, влияющие на время решения:

+ *Число инструкций:* функции с числом базовых инструкций выше $approx 15$ стабильно превышают тайм-аут 300 секунд. Причина --- нелинейная сложность теории массивов (QF\_ABV) при большом числе квантифицированных переменных.

+ *Оптимизация устранения переменных памяти.* До оптимизации функция из 20 инструкций порождала 43 ограничения над массивами; после оптимизации --- 10. Снижение числа array-ограничений в 4,3 раза кратно ускоряет поиск инварианта.

+ *Инлайнинг правил инструкций.* Вместо обращения к инструкционным отношениям через `declare-rel` тела правил встраиваются непосредственно в программное правило, устраняя слой вложенности CHC-системы и упрощая работу алгоритма Spacer @cite14.

== Масштабная верификация с помощью Csmith

Для систематической оценки применимости подхода к реальным компиляторным оптимизациям была разработана система автоматизированного тестирования на основе генератора случайных программ Csmith @cite3. Конвейер `csmith_ricover_e2e.py` функционирует следующим образом:

```
csmith --no-safe-math --no-pointers --no-global-variables
    -> program.c
    -> clang -O0 -Xclang -disable-O0-optnone -emit-llvm -S -> base.ll
    -> llc -O1 -> base.s
    -> opt -passes='<pipeline>' -S -> opt.ll -> llc -O1 -> opt.s
    -> пофункциональный diff (пропуск: call, relocations, >max-instrs)
    -> RICOVER check-equiv -> .smt2
    -> z3 -T:300 -> sat/unsat/timeout
```

Флаг `-Xclang -disable-O0-optnone` необходим, чтобы `opt` проходы фактически срабатывали на O0 IR. Флаг `--no-global-variables` позволяет избежать PC-relative relocations (`auipc`/`%pcrel`), которые RICOVER пока не поддерживает.

*Тестируемые проходы LLVM:* instcombine, simplifycfg, gvn, dse, licm, indvars, loop-rotate, loop-unroll, loop-deletion --- всего 9 конвейеров.

*Воронка отбора функций.* Из 185 функций с различиями между базовым и оптимизированным вариантами (20 программ, проход instcombine):

#figure(
  caption: [Воронка отбора функций (проход instcombine, 20 программ)],
  table(
    columns: (auto, auto, auto),
    table.header([Стадия фильтрации], [Количество], [Доля от различающихся]),
    [Функции с различиями], [185], [100%],
    [Заблокированы инструкцией `call`], [152], [82%],
    [Заблокированы PC-relative relocation], [136], [73%],
    [Без вызовов и relocations], [20], [11%],
    [В пределах размера + поддерживаемые инструкции], [11], [6%],
  ),
) <table-funnel>

*Результаты с упрощёнными флагами Csmith.* При использовании флагов `--no-arrays --no-structs --no-pointers` за серию из 100 программ (seeds 300--399, проход instcombine) было получено 153 пригодных функции. Из 10 функций с наименьшим числом инструкций:

#figure(
  caption: [Результаты верификации функций Csmith (instcombine, seeds 300--399)],
  table(
    columns: (auto, auto, auto, auto),
    table.header([Функция], [src / tgt инструкций], [Результат Z3], [Время]),
    [func\_36], [10 / 8], [*unsat*], [27 с],
    [func\_38], [11 / 9], [*unsat*], [28 с],
    [func\_29], [11 / 9], [*unsat*], [29 с],
    [func\_43], [11 / 9], [*unsat*], [31 с],
    [func\_88], [11 / 9], [*unsat*], [42 с],
    [func\_12], [13 / 9], [*unsat*], [77 с],
    [func\_39], [11 / 9], [*unsat*], [160 с],
    [func\_46], [10 / 8], [*unsat*], [276 с],
    [func\_17], [13 / 9], [timeout], [300 с],
    [func\_153], [12 / 8], [timeout], [300 с],
  ),
) <table-csmith>

*7 из 10 функций подтверждены как эквивалентные* (`unsat`) в пределах 300-секундного тайм-аута; 2 функции превысили тайм-аут. Это существенное улучшение по сравнению с базовой конфигурацией без оптимизаций переменных памяти (2 из 10 при том же тайм-ауте).

*Результаты прогона по всем 9 проходам* (4 программы):

#figure(
  caption: [Результаты прогона по всем 9 проходам LLVM],
  table(
    columns: (auto, auto, auto, auto, auto),
    table.header([Программа], [Функция], [src / tgt инструкций], [Проходов], [Результат]),
    [prog\_1], [func\_1], [15 / 9], [9], [*9/9 EQUIV*],
    [prog\_2], [func\_1], [30 / 9], [9], [*9/9 EQUIV*],
    [prog\_3], [func\_16], [15 / 9], [6], [*6/6 EQUIV*],
  ),
) <table-allpasses>

24 из 24 тестируемых случаев подтверждены как эквивалентные. Ни одного тайм-аута и ни одного `sat` --- все 9 проходов LLVM производят семантически корректные оптимизации на простых регистровых функциях.

== Ограничения и направления развития

Известные ограничения прототипа:

+ *Масштабируемость решателя.* Граница сложности --- около 15 базовых инструкций: функции выше этого предела стабильно укладываются в тайм-аут 300 секунд. Направление развития: применение Eldarica @cite13 как альтернативного CHC-решателя с иной стратегией поиска инварианта.

+ *Функции с вызовами (call).* Csmith генерирует функции, вызывающие другие функции программы. RICOVER верифицирует одиночные функции и не поддерживает межпроцедурный анализ. Направление: compositional verification, верификация пре-/постусловий вызываемых функций.

+ *PC-relative relocations.* Функции, использующие `auipc`/`%pcrel` для загрузки глобальных переменных, не поддерживаются. Направление: расширение модели состояния на сегмент глобальных данных.

+ *Ложные срабатывания (false positives).* Идентифицировано два класса:
  - _Предусловия ABI:_ RICOVER не ограничивает начальные значения регистров вызывным соглашением. Функции с узкими типами параметров (`uint8_t`, `int16_t`) могут давать `sat`, потому что O0 явно расширяет знак, а оптимизированная версия предполагает гарантию ABI.
  - _Мёртвые записи в стек по значению:_ при передаче структуры по значению DSE корректно удаляет мёртвую запись в локальную копию структуры, но эта копия находится за пределами приватного фрейма (на стеке вызывающей функции), и RICOVER считает её наблюдаемой.
